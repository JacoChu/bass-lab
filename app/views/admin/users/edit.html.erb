<div class="flex items-center gap-3 mb-6">
  <%= link_to "← 返回", admin_user_path(@user), class: "btn btn-ghost btn-sm" %>
  <h1 class="text-2xl font-bold">編輯使用者</h1>
</div>

<div class="card bg-base-100 shadow border border-base-300 max-w-lg">
  <div class="card-body">
    <p class="text-sm opacity-60 mb-4">Email：<strong><%= @user.email %></strong>（不可修改）</p>
    <%= render "form", user: @user %>
  </div>
</div>
